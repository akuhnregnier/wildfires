[metadata]
license_files = LICENSE

[tool:pytest]
addopts =
    -n4
    --benchmark-sort=fullname
    --benchmark-autosave
    --doctest-modules
    --cov=wildfires
    --ignore=dev
    --ignore=examples
    --maxfail=1
    -r a
    -v
markers =
    slow: mark a test as being very slow.
