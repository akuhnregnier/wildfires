[metadata]
license_files = LICENSE

[tool:pytest]
addopts =
    -n3
    --benchmark-sort=fullname
    --benchmark-autosave
    --doctest-modules
    --cov=wildfires
    -r a
    -v
markers =
    slow: mark a test as being very slow.
